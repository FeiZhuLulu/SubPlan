# No environment variable is required for the public recommender.

# Admin JSON editor is disabled in production by default.
# Enable only for a private deployment or temporary maintenance window.
# ENABLE_ADMIN_API=1
# NEXT_PUBLIC_ENABLE_ADMIN=1
